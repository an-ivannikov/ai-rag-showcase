{
  "name": "@ai-rag-showcase/web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "dev": "nodemon --watch src --exec tsx ./src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "5.9.2"
  }
}